<UserControl x:Class="ProjectCoder.View.CodeEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectCoder.View"
             mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="535" Background="Transparent">
    <UserControl.Resources>
        <Style x:Key="font_style" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Fonts/Roboto-Light.ttf #Roboto"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
        </Style>

        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#674188"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="5 5"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Padding" Value="15 15"/>
            <Setter Property="FontWeight" Value="400"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="5"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Margin="{TemplateBinding Margin}"/>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              SnapsToDevicePixels="True"
                                              RecognizesAccessKey="True"
                                              Margin="{TemplateBinding Padding}"
                                              ContentSource="Header"
                                              Focusable="False"/>

                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                        Value="#BBC1D1"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background"
                                        Value="#ababe3"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Margin="15 0 0 0" FontSize="26" 
                   Foreground="#674188"
                   Text="Редактор кода" Style="{StaticResource font_style}" TextDecorations="{x:Null}"/>
        <StackPanel  Grid.Row="1">   
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                
                <ListView Grid.Column="1" Margin="10 0"                          
                          x:Name="taskListView"
                          Visibility="Hidden"
                          SelectionChanged="taskListView_SelectionChanged">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListViewItem>Задача 1</ListViewItem>
                    <ListViewItem>Задача 2</ListViewItem>
                    <ListViewItem>Задача 3</ListViewItem>
                    <ListViewItem>Задача 4</ListViewItem>
                    <ListViewItem>Задача 5</ListViewItem>
                    <ListViewItem>Задача 6</ListViewItem>
                    <ListViewItem>Задача 7</ListViewItem>
                </ListView>
                <CheckBox Checked="checkTask_Checked" 
                          Unchecked="checkTask_Unchecked" 
                          Content="Решать задачи" x:Name="checkTask" 
                          FontSize="14" Foreground="#674188" />
            </Grid>
            <Grid  x:Name="taskGrid" Height="0">
                <TextBlock x:Name="taskTextBlock"                          
                           TextWrapping="Wrap" FontSize="15" Foreground="#674188"/>
            </Grid>
            <Grid x:Name="richGrid" Background="#DEDDEE" Height="430">              
                <RichTextBox  Margin="10 10" FontSize="14" BorderThickness="0"
                             x:Name="newProject" Foreground="#674188"  />
            </Grid>               
        </StackPanel>      
        <Button x:Name="check" Click="check_Click" Content="Проверить решение" 
                   FontFamily="SemiBold" 
                        FontSize="20"
                        Foreground="White" 
                        Background="#ababe3" 
                        BorderThickness="0" 
                        Width="250" Height="50"
                        Margin="5"
                        Grid.Row="2"/>
    </Grid>
</UserControl>
